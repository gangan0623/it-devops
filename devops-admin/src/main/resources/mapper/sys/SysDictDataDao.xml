<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.leoch.modules.sys.dao.SysDictDataDao">

    <select id="getDictDataList" resultType="net.leoch.modules.sys.entity.DictData">
        select dict_type_id, dict_label, dict_value from sys_dict_data order by dict_type_id, sort
    </select>

    <select id="getDictDataListByType" resultType="net.leoch.modules.sys.entity.DictData">
        select d.dict_type_id, d.dict_label, d.dict_value
        from sys_dict_data d
        join sys_dict_type t on t.id = d.dict_type_id
        where t.dict_type = #{dictType}
        order by d.sort
    </select>

</mapper>
