spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.17.98:3306/devops?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true
    username: root
    password: Leoch.db.2050
    hikari:
      minimum-idle: 5                    # 最小空闲连接（降低资源占用）
      maximum-pool-size: 50              # 最大连接数（开发环境降低）
      connection-timeout: 10000          # 连接超时10s（增加稳定性）
      idle-timeout: 600000               # 空闲连接存活时间10min
      max-lifetime: 1800000              # 连接最大存活时间30min
      connection-test-query: SELECT 1    # 连接测试查询
      validation-timeout: 3000           # 验证超时3s
  data:
    redis:
      database: 0
      host: 192.168.17.121
      port: 6379
      password: Leoch@@20260101
      timeout: 10000ms                   # 命令超时10s（增加稳定性）
      connect-timeout: 5000ms            # 连接超时5s
      lettuce:
        pool:
          max-active: 50                 # 最大连接数（开发环境降低）
          max-wait: 3000ms               # 最大等待时间3s（避免无限阻塞）
          max-idle: 10                   # 最大空闲连接
          min-idle: 2                    # 最小空闲连接（降低资源占用）
          time-between-eviction-runs: 60000ms  # 空闲连接检测间隔1min
knife4j:
  enable: false
  basic:
    enable: false
    username: admin
    password: admin
  setting:
    enableFooter: false